{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["../script.js", ".next", "dist", "esm"]
    },
    "deploy:dev": {
      "dependsOn": ["^deploy:dev"]
    },
    "lint": {}
  }
}
